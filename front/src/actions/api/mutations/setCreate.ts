// import { graphqlOperation, API } from 'aws-amplify';
// import { SetCreateVariables } from 'typings/graphql';
// import { SET_CREATE } from '@mutations';

// /** 学習情報を保存する */
// export const setCreate = async (userId: string, name: string) => {
//   await API.graphql(graphqlOperation(SET_CREATE, {
//     userId,
//     name,
//   } as SetCreateVariables));
// };

// export type SetCreateAction = (userId: string, name: string) => Promise<void>;
